#include <stdio.h>

int main()
{
    // ? Invalid: number ?? variable ??? ? ?? ???
    // int 1stPlayer;

    // ? Correct
    int firstPlayer;

    // ? Valid
    int player_age;

    // ? Invalid: $ ?? ????? variable name ??? ??? ????? ???
    // int $score;

    // ? Correct
    int score;

    // ? Invalid: - ?? ???? minus operator ??? ??
    // int total-marks;

    // ? Correct
    int total_marks;

    // ? Valid
    int userName;

    return 0;
}
